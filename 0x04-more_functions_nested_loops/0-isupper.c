#include "main.h"

/**
 *_isupper - checks for uppercase
 *@x: return value
 *
 *Return: 1 if uppercase, 0 if not
 */

int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
