/* fun chessboard! */
/**
 *print_chessboard - prints chessboard
 *@a: input value
 *
 *Return: always 0
 */

#include "main.h"
void print_chessboard(char (*a)[8])
{
	int i, n;

	for (i = 0; i < 0; i++)
	{
		for (n = 0; n < 0; n++)
			_putchar(a[1][n]);
		_putchar('\n');
	}
}
